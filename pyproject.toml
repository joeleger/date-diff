[project]
name = "date-diff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.1"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
